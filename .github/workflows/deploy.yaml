name: Deploy Infraestructura AWS

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Traer el código del repo
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Configurar credenciales AWS
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY_ID }}
          aws-region: us-east-1

      # 3️⃣ Validar el template CloudFormation
      - name: Validate CloudFormation template
        run: |
          echo "Validando template..."
          aws cloudformation validate-template \
            --template-body file://main.yaml

      # 4️⃣ Desplegar el stack
      - name: Deploy CloudFormation stack
        run: |
          STACK_NAME=NuevaInfraestructura
          TEMPLATE_FILE=main.yaml

          echo "Desplegando stack $STACK_NAME..."
          aws cloudformation deploy \
            --stack-name $STACK_NAME \
            --template-file $TEMPLATE_FILE \
            --capabilities CAPABILITY_NAMED_IAM \
            --no-fail-on-empty-changeset \
            --region us-east-1

      # 5️⃣ Mostrar los outputs del stack
      - name: Show Stack Outputs
        run: |
          STACK_NAME=NuevaInfraestructura
          echo "Outputs del stack:"
          aws cloudformation describe-stacks \
            --stack-name $STACK_NAME \
            --query "Stacks[0].Outputs" \
            --output table
